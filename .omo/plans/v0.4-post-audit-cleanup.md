# v0.4 Post-Audit Cleanup

## TL;DR

> **Quick Summary**: 12-itemów z audytu całego repozytorium — SonarQube smells, CI format fix, WireMock upgrade, deduplikacja walidatorów, testy API, dokumentacja. 4 fale, ~10h.
>
> **Deliverables**:
> - SonarQube: 28 smells → 0
> - CI: format check działa na GitHub Actions
> - WireMock 2.6.0 → 2.9.0
> - Benchmark validatory: 4 pliki → 1 base + 4 warianty
> - Testy API dla ChatEndpoints + TraceEndpoints
> - CHANGELOG + docs zaktualizowane
>
> **Estimated Effort**: ~10h (4 fale)
> **Parallel Execution**: YES

---

## Context

### Background
Po 44 commitach na `cleanup/v0.4-dead-code-benchmark-fix`, repozytorium jest w dobrym stanie:
- 0 TODO/FIXME/HACK
- 0 Obsolete/Deprecated
- 283 unit testów, 53 integracyjnych
- Build 0W/0E, SonarQube 0 bugs, 0 vulnerabilities
- PR #11: 0 CodeRabbit unresolved

Pozostało 12 itemów do doprowadzenia repozytorium do stanu "production-ready clean".

---

## Work Objectives

### Core Objective
Doprowadzić repozytorium do stanu zero-warningów, zero-code-smells, pełnej dokumentacji.

### Concrete Deliverables
- SonarQube: 0 code smells
- CI: format check działa (nie timeout na build-host)
- WireMock: zaktualizowany do 2.9.0
- 4 benchmark walidatory zdeduplikowane do wspólnej bazy
- Testy API dla obu endpointów
- CHANGELOG + docs zaktualizowane o ostatni fix
- 4 stare branche usunięte
- boulder.json wyczyszczony (3 active → completed)

### Definition of Done
- `dotnet build -c Release` → 0W/0E
- `dotnet test` → 283+/283+ pass
- SonarQube: code_smells = 0
- CI: wszystkie kroki zielone
- PR #11 zmergowany do main

---

## Tasks

### Wave 1 — Quick Wins (2h)

- [x] 1. Wyczyść boulder.json — 3 active → completed
  **Co**: `dotnet-upgrade-8-to-10`, `handcodec-v0-4-upgrade`, `e2e-checkpoint-comprehensive` mają `status: "active"` ale wszystkie taski są done
  **Plik**: `.omo/boulder.json`
  **Weryfikacja**: `jq '.works | to_entries[] | select(.value.status == "active")' .omo/boulder.json` → 0 wyników

- [x] 2. WireMock 2.6.0 → 2.9.0
  **Co**: Aktualizacja `WireMock.Net` w `tests/HybridTherapist.Integration/HybridTherapist.Integration.csproj`
  **Plik**: `tests/HybridTherapist.Integration/HybridTherapist.Integration.csproj`
  **Weryfikacja**: `dotnet test tests/HybridTherapist.Integration -c Release --filter "Cassette"` → pass

- [x] 3. `dotnet test` 18.5.1 → 18.6.0 w obu projektach testowych
  **Pliki**: `tests/HybridTherapist.Tests/HybridTherapist.Tests.csproj`, `tests/HybridTherapist.Integration/HybridTherapist.Integration.csproj`
  **Weryfikacja**: `dotnet test` → 283/283 pass

- [x] 4. Usuń 4 stare branche (lokalnie + remote)
  **Branche**: `codec-g-benchmark`, `feat/ci-cd-standard-v2.0.0`, `feat/gh-packages-nuget`, `feat/therapy-logic-fixes`
  **Weryfikacja**: `git branch -a | wc -l` → zmniejszone o 4

- [ ] 5. Zainstaluj csharp-ls (BLOCKED: pakiet niekompatybilny z .NET 10 — brak DotnetToolSettings.xml)
  **Komenda**: `dotnet tool install -g csharp-ls`

### Wave 2 — SonarQube + CI (3h)

- [x] 6. CI: napraw dotnet format w GitHub Actions (ALREADY FIXED — CI ma setup-dotnet@v5 z 10.0.x, format działa po fixie WHITESPACE w 20b620c)

- [ ] 7. SonarQube: 28 code smells → 0
  **Główne źródła**:
  - Duplikacja w BenchmarkValidator ×4 → deduplikacja (Wave 3)
  - Brak testów dla niektórych klas
  - Drobne style issues
  **Strategia**: Po deduplikacji (Wave 3), pozostałe smells będą głównie drobnymi fixami
  **Weryfikacja**: SonarQube API → `code_smells: 0`

### Wave 3 — Deduplikacja (3h)

- [ ] 8. Wydziel `HandBenchmarkValidatorBase` z 4 walidatorów benchmark
  **Problem**: `HandJsonBenchmarkValidator.cs` (280 linii), `HandSemanticBenchmarkValidator.cs` (272), `HandPlaintextBenchmarkValidator.cs` (211), `HandBenchmarkValidator.cs` (323) dzielą ~70% identycznych metod
  **Wspólne metody**: `ValidateExpectedQuality`, `ValidateL4Input`, `L4ForbiddenInstructionMarkers`, `PolishStopwords`, `LooksPolish`, `WireOrOutput`, `SingleLayer`, `CountTokenEquivalents`
  **Różne**: `CalculateTokenSavings` (specyficzne dla wariantu), konwersje formatów
  **Podejście**:
    1. Stwórz `HandBenchmarkValidatorBase` z metodami wspólnymi
    2. Każdy wariant dziedziczy base i implementuje tylko `CalculateTokenSavings` + konwersje
    3. Base ma `protected abstract TokenSavingsMetrics CalculateTokenSavings(BenchmarkRun)`
  **Pliki**: 4 istniejące → 1 nowy base + 4 zmodyfikowane
  **Weryfikacja**: `dotnet test --filter "HandBenchmark\|HandJson\|HandSemantic\|HandPlaintext"` → wszystkie pass

### Wave 4 — Testy + Dokumentacja (2h)

- [ ] 9. Testy API dla ChatEndpoints
  **Co**: `WebApplicationFactory<Program>` + WireMock (mock Ollama)
  **Minimalne scenariusze**: POST /v1/chat/completions z poprawnym body → 200 + JSON, puste body → 400, invalid JSON → 400, brak model → 400
  **Plik**: `tests/HybridTherapist.Integration/ChatEndpointsTests.cs`
  **Weryfikacja**: `dotnet test --filter "ChatEndpoints"` → 4+/4+ pass

- [ ] 10. Testy API dla TraceEndpoints
  **Co**: GET /v1/trace/{sessionId} → 200 + JSON z events, nieistniejący sessionId → 200 + pusta lista
  **Plik**: `tests/HybridTherapist.Integration/TraceEndpointsTests.cs`
  **Weryfikacja**: `dotnet test --filter "TraceEndpoints"` → 2+/2+ pass

- [ ] 11. Zaktualizuj CHANGELOG.md o ostatni fix (QualityValidator therapeutic substance)
  **Plik**: `CHANGELOG.md`
  **Weryfikacja**: `head -30 CHANGELOG.md` zawiera wpis o fixie z 2026-06-07

- [ ] 12. Zaktualizuj docs/architecture.md o L6 hardenowanie + QualityValidator
  **Plik**: `docs/architecture.md`
  **Weryfikacja**: `grep -c "reflection" docs/architecture.md` > 0

---

## Final Verification Wave

- [ ] F1. **Build** — `dotnet build -c Release` → 0W/0E
- [ ] F2. **Unit Tests** — `dotnet test tests/HybridTherapist.Tests -c Release` → 283+/283+
- [ ] F3. **Integration Tests** — `dotnet test tests/HybridTherapist.Integration -c Release` → wszystkie pass
- [ ] F4. **SonarQube** — `curl -s ... | jq '.component.measures[] | select(.metric=="code_smells")'` → value: "0"
- [ ] F5. **CI** — GitHub Actions zielone na wszystkich krokach

---

## Execution Strategy

```
Wave 1: Quick Wins (2h) — MAX PARALLEL (4 items)
├── boulder.json cleanup
├── WireMock upgrade
├── Test.Sdk upgrade
└── Stale branches deletion

Wave 2: CI + SonarQube (3h)
├── CI format fix
└── SonarQube smells (after Wave 3 dedup)

Wave 3: Deduplikacja (3h)
└── HandBenchmarkValidatorBase (↓ SonarQube smells by ~15)

Wave 4: Testy + Dokumentacja (2h)
├── ChatEndpoints API tests
├── TraceEndpoints API tests
├── CHANGELOG update
└── Architecture docs update

Critical Path: Wave 1 → Wave 3 → Wave 2 → Wave 4
```
