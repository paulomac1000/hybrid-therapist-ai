version: 1.3.3
cache: true
fileConfig:
  strategy: local
registration:
  socialLogins: []
endpoints:
  custom:
    # ── Hybrydowy Psycholog → therapist container :8080 ──────────────────────
    - name: Hybrydowy Psycholog
      apiKey: dummy
      baseURL: http://therapist:8080/v1/
      models:
        default:
          - hybrid-therapist
      titleConvo: true
      titleModel: current_model
      modelDisplayLabel: "🧠 Psycholog AI"
      dropParams:
        - temperature
        - top_p
        - frequency_penalty
        - presence_penalty
